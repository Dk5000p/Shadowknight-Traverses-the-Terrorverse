using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ClownLevelset : MonoBehaviour {

	// Use this for initialization
	void Start () {
        PlayerPrefs.SetInt("Clownlevel",1);
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
